import React from "react";

const Header: React.FC = () => (
  <header className="flex fle-row w-full justify-center items-center h-[50px] bg-blue-600 text-white">
    <h1 className="text-xl">Product Management Dashboard</h1>
  </header>
);

export default Header;
